---
"@xyflow/vue": minor
---

Added a `debug` prop. When `true`, Vue Flow logs its events to the console as they fire (high-frequency events like move/drag/hover are skipped) — useful for seeing which events run.
