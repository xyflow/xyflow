name: 'CI setup'

runs:
  using: 'composite'
  steps:
    - name: Setup pnpm
      uses: pnpm/action-setup@v6
    - name: Setup node.js
      uses: actions/setup-node@v6
      with:
        node-version: 20.x
    - name: Install dependencies
      run: pnpm install
      shell: bash
