import { Meta } from '@storybook/blocks';

<Meta title="React Flow/Types/BackgroundVariant" />

# BackgroundVariant

The three variants are exported as an enum for convenience. You can either import the enum and use it like `BackgroundVariant.Lines` or you can use the raw string value directly.

```ts
export enum BackgroundVariant {
  Lines = 'lines',
  Dots = 'dots',
  Cross = 'cross',
}
```

Used by: [Components/Background](?path=/story/react-flow-components-background--lines)

See also: [Components/Background](?path=/story/react-flow-components-background--lines)

Reference: React Flow docs for BackgroundVariant [link](https://reactflow.dev/api-reference/types/background-variant)
